# ============================================================================
# SolMate Production Environment Variables
# ============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control
# ============================================================================

# ----------------------------------------------------------------------------
# Application
# ----------------------------------------------------------------------------
NODE_ENV=production

# ----------------------------------------------------------------------------
# MongoDB Configuration
# ----------------------------------------------------------------------------
MONGO_URL=mongodb://localhost:27017
DB_NAME=solmate

# ----------------------------------------------------------------------------
# Public URLs (REQUIRED)
# ----------------------------------------------------------------------------
# Your production domain (no trailing slash)
NEXT_PUBLIC_BASE_URL=https://playsolmates.app

# ----------------------------------------------------------------------------
# NextAuth Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Must match your production domain
NEXTAUTH_URL=https://playsolmates.app

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# ----------------------------------------------------------------------------
# SMTP Configuration - Email Magic Link (REQUIRED for email auth)
# ----------------------------------------------------------------------------
# For Zoho Mail:
#   SMTP_HOST=smtp.zoho.eu
#   SMTP_PORT=465
# For Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
# For SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587

SMTP_HOST=smtp.zoho.eu
SMTP_PORT=465
SMTP_USER=noreply@playsolmates.app
SMTP_PASS=
EMAIL_FROM=SolMate <noreply@playsolmates.app>

# ----------------------------------------------------------------------------
# OAuth Providers (OPTIONAL - only configure what you need)
# ----------------------------------------------------------------------------

# Google OAuth
# Create at: https://console.cloud.google.com/apis/credentials
# Callback URL: https://playsolmates.app/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Facebook OAuth  
# Create at: https://developers.facebook.com/apps
# Callback URL: https://playsolmates.app/api/auth/callback/facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Twitter/X OAuth 2.0
# Create at: https://developer.twitter.com/en/portal/dashboard
# Callback URL: https://playsolmates.app/api/auth/callback/twitter
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# ----------------------------------------------------------------------------
# Solana Configuration
# ----------------------------------------------------------------------------
# Options: devnet, mainnet-beta
NEXT_PUBLIC_SOLANA_CLUSTER=mainnet-beta

# RPC URLs
# Devnet: https://api.devnet.solana.com
# Mainnet: https://api.mainnet-beta.solana.com (or use Helius/Quicknode for better performance)
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Developer wallet for receiving VIP payments
NEXT_PUBLIC_DEVELOPER_WALLET=
DEVELOPER_WALLET=

# USDC Mint addresses (don't change unless you know what you're doing)
NEXT_PUBLIC_USDC_MINT_DEVNET=4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU
NEXT_PUBLIC_USDC_MINT_MAINNET=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v

# ----------------------------------------------------------------------------
# JWT Secret for Wallet Auth (REQUIRED)
# ----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=

# ----------------------------------------------------------------------------
# VIP Pricing
# ----------------------------------------------------------------------------
VIP_PRICE_USDC=6.99

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------
CORS_ORIGINS=https://playsolmates.app

# ----------------------------------------------------------------------------
# Debug Flags (PRODUCTION: keep these false)
# ----------------------------------------------------------------------------
NEXT_PUBLIC_DEBUG_QUICKCHAT=false
